# Issue Tracker

Issues and specs for this repository are tracked locally as markdown files.

- They are stored under `.scratch/<feature>/issues/`.
- This is a local-only workflow that does not interact with a remote issue tracker.
